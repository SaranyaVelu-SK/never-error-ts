## Annotations
* variableName:type
* for Arrays -> **variableName:type[]**   or   **variableName:Array<type>**
* If your file has NO import or export, it is treated as a global script.It can clash with existing global variables like: *name, status, event, length*
